<div class="container">
  <div class="row">
    <h3 class="under-border">会員情報一覧</h3>
  </div>


    <div class="col-xs-11">
    <table class="table">
      <thead>
        <tr>
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= link_to admin_user_path(user) do %>
              <%= user.last_name %>
              <%= user.first_name %>
              <% end %>
            </td>
          <td><%= user.email %></td>
          <td><% if user.status == true %>
              有効会員
              <% else %>
              退会済み
              <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>



  <div class="container">
    <div class="row">
    <div class="col-md-4 py-2 mt-5">
    <%= paginate @users %>
    </div>
    </div>
  </div> 
</div>