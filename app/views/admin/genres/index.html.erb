<div class="container">
  <div class="row">
    <h3 class="under-border">ジャンル追加・一覧画面</h3>
    <div class="col-xs-6 genre-form">
      <%= form_with url: admin_genres_path,model: @genre,local: true do |f| %>
        <%= render 'layouts/error_messages', model: @genre %>
        <%= f.text_field :genre%> 
        <%= f.radio_button :display_status, true %>
        <%= f.label :display_status, t('enums.genre.display_status.true') %>
        <%= f.radio_button :display_status, false %>
        <%= f.label :display_status, t('enums.genre.display_status.false') %>
        <%= f.submit "追加" ,class:"btn btn-primary" %>
      <% end %>
    </div>

    <div class="col-xs-10">
      <table class="table">
        <thead class="thead-light">
          <tr class="active">
            <th>ジャンル</th>
            <th>状態</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.genre %></td>
              <td><%= t("enums.genre.display_status.#{genre.display_status}") %></td>
              <td><%= link_to "編集する", edit_admin_genre_path(genre.id) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>    
    </div>  
  </div>        
  <%= paginate @genres %>  
</div>
